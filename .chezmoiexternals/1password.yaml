{{ if eq .chezmoi.os "linux" }}
{{- $opVersion := "2.32.0" -}}
{{- $arch := .chezmoi.arch | replace "aarch" "arm" -}}
".local/bin/op":
  type: archive-file
  executable: true
  url: "https://cache.agilebits.com/dist/1P/op/pkg/v2/{{ $opVersion }}/op_linux_{{ $arch }}_v{{ $opVersion }}.zip"
{{ end }}
